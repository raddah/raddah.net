---
import PageLayout from '@/layouts/PageLayout.astro';
import DiwanList from '@/components/blog/DiwanList.astro';
import SearchDialog from '@/components/global/SearchDialog.astro';
import { getPostsByLocale } from '@/utils/content';

const locale = 'en';
const posts = await getPostsByLocale(locale);
---

<PageLayout
  title="Articles — raddah.net"
  description="All published articles in Raddah's Diwan"
  locale={locale}
>
  <SearchDialog locale={locale} />
  <div class="blog-listing container">
    <header class="blog-listing__head">
      <h1>Diwan</h1>
      <p class="count">{posts.length} articles</p>
    </header>
    <DiwanList posts={posts} locale={locale} startNum={1} showHeader={false} />
  </div>
</PageLayout>

<style>
  .blog-listing { padding-block: var(--s-8); }
  .blog-listing__head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding-block-end: var(--s-4);
    border-block-end: 1px solid var(--ink);
    margin-block-end: var(--s-3);
  }
  .blog-listing__head h1 {
    font-family: var(--font-en-display);
    font-size: var(--t-4xl);
    font-style: italic;
    font-weight: 400;
  }
  .count {
    font-family: var(--font-mono);
    font-size: var(--t-xs);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--ink-mute);
  }
</style>
